<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.ramitas.practica3app">
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"/>
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"/>


    <application

        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.DayNight.DarkActionBar"
        android:networkSecurityConfig="@xml/network_security_config"> <!-- Corregido aquí -->

        <activity
            android:name=".view.LoginActivity"
            android:exported="true"> <!-- Agregar android:exported -->
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".view.RegisterActivity"
            android:exported="true" /> <!-- Agregar android:exported -->

        <activity
            android:name=".view.UserListActivity"
            android:exported="true" /> <!-- Agregar android:exported -->

        <activity
            android:name=".view.ProfileUserActivity"
        android:exported="true" />

        <activity
            android:name=".view.AdminActivity"
        android:exported="true" />

        <activity
            android:name=".view.UpdateUser"
            android:exported="true" />

        <activity android:name=".view.AddUser" />
        <activity android:name=".view.DeleteUser" /> <!-- Asegúrate de incluir esta línea -->
        <activity android:name=".view.ViewRecords" />

    </application>

</manifest>
